<Page
    x:Class="NuHue.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NuHue"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="Maroon">
        <Grid.RowDefinitions>
            <RowDefinition Height="160*"/>
            <RowDefinition Height="160*"/>
            <RowDefinition Height="160*"/>
            <RowDefinition Height="160*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button x:Name="onButton" Content="On" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="onButton_Click" Margin="10"/>
        <Button x:Name="offButton" Content="Off" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="offButton_Click" Margin="10" Grid.Column="1" BorderThickness="0"/>
    </Grid>
</Page>
